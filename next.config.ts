import type { NextConfig } from 'next';

const nextConfig: NextConfig = {
  /* config options here */
  images: {
    domains: ['images.unsplash.com', 'i.imgur.com', 'picsum.photos', 'loremflickr.com', 'placeimg.com'],
  },
};

export default nextConfig;
